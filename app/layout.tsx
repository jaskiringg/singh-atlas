import type { Metadata } from "next";
import { GeistSans } from "geist/font/sans";
import { GeistMono } from "geist/font/mono";
import "./globals.css";

export const metadata: Metadata = {
  title: "Jaskirat Singh — Systems",
  description:
    "Systems thinker who ships in production environments. Enterprise integrations, production operators, local-first experiments.",
  openGraph: {
    title: "Jaskirat Singh — Atlas",
    description: "Turns ambiguity into well-designed systems.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${GeistSans.variable} ${GeistMono.variable}`}>
      <body className="min-h-screen antialiased">{children}</body>
    </html>
  );
}
